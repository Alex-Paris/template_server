model User {
  id         String   @id
  name       String
  email      String   @unique
  password   String   @ignore
  avatar     String?
  created_at DateTime @default(now())
  updated_at DateTime @default(now()) @updatedAt
}
