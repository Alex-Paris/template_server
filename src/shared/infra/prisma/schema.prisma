generator client {
  provider = "prisma-client-js"
}

datasource db {
  provider = "postgresql"
  url      = env("DATABASE_URL")
}

model Model {
  id          String  @id
  name        String
  description String?
}

model User {
  id    String  @id
  name  String
  email String?
}
